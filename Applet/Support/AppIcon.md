# Noodle Applet icon

Generated with the built-in image tool, using `Support/AppIcon.png` and
`Computer/Support/AppIcon.png` as family references. Final asset: `AppIcon.png`.

Prompt: Match the existing Noodle and Noodle Computer icon family exactly: vivid
blue rounded square and warm cream curved lowercase n. For Noodle Applet, place
the n inside a simple application-window outline with a title bar and two small
cream controls. Preserve the family palette, stroke weight, minimal composition,
and rounded geometry. No text, orange tiles, mockup, or shadows outside the icon.

A targeted edit requested true transparency outside the blue rounded square.
The tool returned opaque checkerboard pixels twice. With the user's explicit
approval, local processing removed only the connected exterior neutral pixels
and saved a real alpha channel. The enclosed mark and blue artwork are unchanged.

Refinement prompt: Edit only the Computer monitor into an application window;
preserve the reference blue gradients, cream texture, corner geometry, and 3%
transparent padding. Center the symbol. Match its Dock scale and subtle finish.
The selected refinement again required the approved exterior-alpha cleanup.

Final tone prompt: Match the Computer tile luminance and shading, using a darker
royal/cobalt base near RGB(28,53,187), restrained highlights near RGB(32,72,220),
and the existing cream window mark. Keep the geometry and transparent padding.
Built-in image editing was used; only the approved exterior-alpha cleanup was local.
