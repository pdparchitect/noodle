#!/bin/bash

set -euo pipefail

exec kitty \
    --title "${DESKTOP_TITLE:-Agent Desktop}" \
    --override remember_window_size=no \
    --override initial_window_width=100c \
    --override initial_window_height=34c \
    -e bash
